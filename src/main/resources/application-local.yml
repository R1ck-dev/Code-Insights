spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/codeinsights
    username: ${DB_USER:codeinsights_admin}
    password: ${DB_PASSWORD:local_password}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  mail:
    host: localhost
    port: 1026
    username: ""
    password: ""
